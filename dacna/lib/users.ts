/**
 * Deprecated: lib/users.ts is no longer used.
 * Authentication is handled via backend APIs in `lib/auth-api.ts` and `lib/user-api.ts`.
 * This file is intentionally left nearly empty to avoid accidental usage.
 */

export {};
